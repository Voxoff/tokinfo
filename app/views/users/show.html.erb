<div class="background-design">
  <div class="navbar-offset"></div>
  <div class="show_container">
    <div class="show_sub_container">
      <h1 class="white"><%= @user.first_name %><%= @user.last_name %></h1>
      <h3 class="shadow-hover"><%= @user.email %></h3>
    </div>
    <%= cl_image_tag @user.photo, height: 200, width: 230, crop: :fill, id: "user-img"%>
  </div>
  <hr class="line">
</div>
<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-3">
      <h4 class="offerings">Businesses:</h4>
      <% @user.businesses.each do |business| %>
        <%= link_to business_path(business) do %>
          <div class="card">
            <br>
            <%= cl_image_tag business.photo, height: 150, width: 200, crop: :fill %>
            <h3><%= business.name %></h3>
            <p><%= business.email %></p>
          </div>
        <% end %>
      <% end %>
    </div>
    <div class="col-xs-12 col-sm-3">
      <h4 class="offerings">Token Offerings:</h4>
        <% @user.tokens.each do |token| %>
        <%= link_to token_path(token) do %>
          <div class="card">
            <br>
            <%= cl_image_tag token.photo, height: 150, width: 200, crop: :fill %>
            <h3><%= token.name %></h3>
          </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
